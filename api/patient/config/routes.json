{
  "routes": [
    {
      "method": "GET",
      "path": "/patients",
      "handler": "Patient.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/patients/count",
      "handler": "Patient.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/patients/:_id",
      "handler": "Patient.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/patients",
      "handler": "Patient.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/patients/:_id",
      "handler": "Patient.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/patients/:_id",
      "handler": "Patient.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
